{
  "name": "thrmnn.github.io",
  "version": "1.0.0",
  "description": "Personal website built with Astro",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "node scripts/ensure-dist.mjs && astro check && node scripts/check-build.mjs",
    "preflight": "rm -rf dist && npm run build && npm test",
    "install-hooks": "node scripts/install-hooks.mjs"
  },
  "type": "module",
  "dependencies": {
    "@astrojs/mdx": "^5.0.2",
    "@astrojs/rss": "^4.0.17",
    "@astrojs/sitemap": "^3.7.1",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.0.7",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^5.9.3"
  }
}
