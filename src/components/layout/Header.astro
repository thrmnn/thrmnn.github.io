---
import { navLinks, mobileNavLinks } from '../../data/navigation';
import ThemeToggle from '../ui/ThemeToggle.astro';

const currentPath = Astro.url.pathname;
const isActive = (href: string) =>
  href === currentPath ||
  (href !== '/' && !href.startsWith('/#') && currentPath.startsWith(href));

// Live readout initial text: on the homepage this is overwritten by the
// section IntersectionObserver as the user scrolls; on every other route
// it's just the route name and stays static.
const routeLabels: Record<string, string> = {
  '/': 'home',
  '/projects/': 'projects',
  '/consulting/': 'consulting',
  '/cv/': 'cv',
  '/now/': 'now',
};
const routeLabel =
  routeLabels[currentPath] ?? currentPath.replace(/^\/|\/$/g, '').split('/')[0] ?? 'page';
---

<header class="site-header" data-header data-open="false">
  <div class="header-pill">
    <div class="header-inner">
      <!-- Left: monogram (also the Home affordance) + live readout -->
      <div class="header-brand">
        <a href="/" aria-label="Théo Alessandro Hermann — Home" class="monogram-link">
          <span class="monogram" aria-hidden="true">
            <span class="mono-ch" style="--i:0">T</span>
            <span class="mono-ch" style="--i:1">·</span>
            <span class="mono-ch" style="--i:2">A</span>
            <span class="mono-ch" style="--i:3">·</span>
            <span class="mono-ch" style="--i:4">H</span>
          </span>
        </a>
        <!-- Names the section/route in view. aria-hidden — purely a chrome
             instrument, not content; visible only in .condensed. -->
        <span class="header-readout" aria-hidden="true" data-readout>— {routeLabel}</span>
      </div>

      <!-- Desktop nav -->
      <nav class="header-nav" aria-label="Primary">
        <ul>
          {navLinks.map((link) => (
            <li>
              <a
                href={link.href}
                class="nav-link"
                {...(isActive(link.href) ? { 'aria-current': 'page' } : {})}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </nav>

      <!-- Controls -->
      <div class="header-controls">
        <a
          href="/consulting/"
          class="header-cta"
          {...(isActive('/consulting/') ? { 'aria-current': 'page' } : {})}
        >consult</a>
        <ThemeToggle />
        <button
          type="button"
          id="nav-toggle-btn"
          class="nav-burger"
          aria-controls="mobile-menu"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <svg class="burger-open" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>
          <svg class="burger-close" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="mobile-menu">
    <ul>
      {mobileNavLinks.map((link) => (
        <li>
          <a
            href={link.href}
            class="nav-link"
            {...(isActive(link.href) ? { 'aria-current': 'page' } : {})}
          >
            {link.label}
          </a>
        </li>
      ))}
    </ul>
  </div>
</header>

<style>
  /* ============================ Floating instrument pill ============================ */
  .site-header {
    position: fixed;
    top: 0.85rem;
    left: 0;
    right: 0;
    z-index: 50;
    padding: 0 1rem;
    pointer-events: none;          /* clicks pass through gutters, only pill catches */
    transition: top var(--dur-2) var(--ease-out);
  }
  .site-header.condensed { top: 0.55rem; }

  .header-pill {
    position: relative;
    pointer-events: auto;
    max-width: 880px;
    margin: 0 auto;
    border: 1px solid color-mix(in srgb, var(--color-border) 80%, transparent);
    border-radius: 999px;
    background-color: color-mix(in srgb, var(--color-bg-primary) 60%, transparent);
    backdrop-filter: blur(20px) saturate(160%);
    -webkit-backdrop-filter: blur(20px) saturate(160%);
    box-shadow: 0 14px 32px -16px rgba(0, 0, 0, 0.18);
    transition:
      background-color var(--dur-2) var(--ease-out),
      border-color var(--dur-2) var(--ease-out),
      box-shadow var(--dur-2) var(--ease-out);
  }
  /* Scan-progress hairline: consumes --progress (set on scroll, see script
     below) as a 1px accent line inside the pill's bottom edge. Position
     tracking itself is ungated (it's a direct reflection of scroll, not an
     animation); only the smoothing transition is reduced-motion gated. */
  .header-pill::after {
    content: "";
    position: absolute;
    left: 1.1rem;
    right: 1.1rem;
    bottom: 0;
    height: 1px;
    background: var(--color-accent);
    transform-origin: left;
    transform: scaleX(var(--progress, 0));
    pointer-events: none;
  }
  @media (prefers-reduced-motion: no-preference) {
    .header-pill::after {
      transition: transform 0.15s linear;
    }
  }
  .site-header.condensed .header-pill {
    background-color: color-mix(in srgb, var(--color-bg-primary) 82%, transparent);
    border-color: var(--color-border-hover);
    box-shadow: 0 8px 22px -14px rgba(0, 0, 0, 0.22);
  }
  /* Dark-mode tuning — deeper shadow. */
  [data-theme="dark"] .header-pill {
    background-color: color-mix(in srgb, var(--color-bg-primary) 42%, transparent);
    border-color: color-mix(in srgb, var(--color-border) 65%, transparent);
    box-shadow: 0 18px 46px -20px rgba(0, 0, 0, 0.7);
  }
  [data-theme="dark"] .site-header.condensed .header-pill {
    background-color: color-mix(in srgb, var(--color-bg-primary) 75%, transparent);
    border-color: var(--color-border);
    box-shadow: 0 8px 22px -12px rgba(0, 0, 0, 0.6);
  }
  @media (prefers-color-scheme: dark) {
    :root:not([data-theme]) .header-pill {
      background-color: color-mix(in srgb, var(--color-bg-primary) 42%, transparent);
      border-color: color-mix(in srgb, var(--color-border) 65%, transparent);
      box-shadow: 0 18px 46px -20px rgba(0, 0, 0, 0.7);
    }
    :root:not([data-theme]) .site-header.condensed .header-pill {
      background-color: color-mix(in srgb, var(--color-bg-primary) 75%, transparent);
    }
  }
  /* Fallback for browsers without backdrop-filter — bump the bg up so the
     content stays legible without the blur trick. */
  @supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
    .header-pill { background-color: var(--color-bg-primary); }
  }

  .header-inner {
    padding: 0 1rem 0 1.25rem;
    height: 3.1rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
  }

  .header-brand {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    min-width: 0;
  }

  /* Live readout — quiet at the top of the page, only names the section/
     route once the pill has condensed. Instrument chrome, not content. */
  .header-readout {
    font-family: var(--font-mono);
    font-size: 0.62rem;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
    white-space: nowrap;
    opacity: 0;
  }
  .site-header.condensed .header-readout {
    opacity: 1;
  }
  @media (prefers-reduced-motion: no-preference) {
    .header-readout {
      transition: opacity var(--dur-1) var(--ease-out);
    }
  }
  @media (max-width: 767px) {
    .header-readout {
      display: none;
    }
  }

  /* Nav — monospace instrument list */
  .header-nav ul {
    display: flex;
    align-items: center;
    gap: 1.6rem;
  }
  .nav-link {
    position: relative;
    font-family: var(--font-mono);
    font-size: 0.72rem;
    letter-spacing: 0.08em;
    text-transform: lowercase;
    color: var(--color-text-muted);
    transition: color var(--dur-1) var(--ease-out);
    padding: 0.3rem 0;
  }
  .nav-link::before {
    content: "";
    position: absolute;
    left: -0.7rem;
    top: 50%;
    width: 4px;
    height: 4px;
    margin-top: -2px;
    background: var(--color-accent);
    opacity: 0;
    transform: scale(0.4);
    transition: opacity var(--dur-1) var(--ease-out), transform var(--dur-1) var(--ease-out);
  }
  .nav-link:hover {
    color: var(--color-text-secondary);
  }
  .nav-link:hover::before {
    background: transparent;
    box-shadow: inset 0 0 0 1px var(--color-accent);
    opacity: 0.85;
    transform: scale(0.9);
  }
  .nav-link[aria-current='page'] {
    color: var(--color-text-primary);
  }
  .nav-link[aria-current='page']::before {
    opacity: 1;
    transform: scale(1);
  }
  .nav-link:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 4px;
    border-radius: 2px;
    color: var(--color-text-primary);
  }
  .nav-link:focus-visible::before {
    opacity: 1;
    transform: scale(1);
  }

  /* LiDAR scan-beam: a soft vertical band sweeps the label on hover,
     echoing the hero point-cloud excite effect. */
  .nav-link::after {
    content: "";
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(
      90deg,
      transparent 42%,
      color-mix(in srgb, var(--color-accent) 45%, transparent) 50%,
      transparent 58%
    );
    background-size: 250% 100%;
    background-position: 130% 0;
    opacity: 0;
  }
  @media (prefers-reduced-motion: no-preference) {
    .nav-link:hover::after,
    .nav-link:focus-visible::after {
      animation: nav-scan 0.55s var(--ease-out) 1;
    }
  }
  @keyframes nav-scan {
    from {
      background-position: 130% 0;
      opacity: 0.95;
    }
    to {
      background-position: -30% 0;
      opacity: 0;
    }
  }

  .header-controls {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  /* Instrument-idiom CTA pill — additive, not part of navLinks. */
  .header-cta {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    letter-spacing: 0.08em;
    text-transform: lowercase;
    color: var(--color-text-secondary);
    padding: 0.32rem 0.65rem;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    white-space: nowrap;
    transition:
      color var(--dur-1) var(--ease-out),
      border-color var(--dur-1) var(--ease-out),
      background-color var(--dur-1) var(--ease-out);
  }
  .header-cta:hover,
  .header-cta[aria-current='page'] {
    color: var(--color-text-primary);
    border-color: var(--color-accent);
    background-color: color-mix(in srgb, var(--color-accent) 10%, transparent);
  }
  .header-cta:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
  }

  /* Burger */
  .nav-burger {
    display: none;
    cursor: pointer;
    color: var(--color-text-secondary);
    padding: 0.65rem;
    min-width: 44px;
    min-height: 44px;
    align-items: center;
    justify-content: center;
    margin-right: -0.35rem;
    background: transparent;
    border: 0;
    appearance: none;
    -webkit-appearance: none;
  }
  .nav-burger svg { width: 1.35rem; height: 1.35rem; display: block; transition: transform var(--dur-1) var(--ease-out); }
  .nav-burger:hover { color: var(--color-text-primary); }
  .nav-burger:active {
    background-color: color-mix(in srgb, var(--color-accent) 14%, transparent);
    border-radius: 6px;
  }
  .nav-burger:active svg { transform: scale(0.92); }
  .nav-burger:focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 2px;
    border-radius: 4px;
  }
  @media (prefers-reduced-motion: reduce) {
    .nav-burger svg, .nav-burger:active svg { transition: none; transform: none; }
  }
  .burger-close { display: none; }
  .site-header[data-open="true"] .nav-burger .burger-open { display: none; }
  .site-header[data-open="true"] .nav-burger .burger-close { display: block; }

  /* Mobile menu */
  .mobile-menu {
    pointer-events: auto;
    display: none;
    max-width: 880px;
    margin: 0.55rem auto 0;
    padding: 0;
    border: 1px solid var(--color-border-hover);
    border-radius: 14px;
    background-color: color-mix(in srgb, var(--color-bg-primary) 92%, transparent);
    backdrop-filter: blur(14px) saturate(140%);
    -webkit-backdrop-filter: blur(14px) saturate(140%);
    box-shadow: 0 14px 40px -16px rgba(0, 0, 0, 0.55);
  }
  .site-header[data-open="true"] .mobile-menu { display: block; }
  .mobile-menu ul {
    padding: 0.85rem 1.1rem 1rem;
    display: flex;
    flex-direction: column;
    gap: 0.55rem;
  }

  /* Responsive */
  @media (max-width: 767px) {
    .header-nav {
      display: none;
    }
    .nav-burger { display: flex; }
    .mobile-menu .nav-link {
      min-height: 44px;
      display: flex;
      align-items: center;
      font-size: 0.9rem;
      padding-left: 0.95rem;
    }
    .mobile-menu .nav-link[aria-current='page'] {
      color: var(--color-text-primary);
    }
    .mobile-menu .nav-link[aria-current='page']::before {
      left: 0;
      opacity: 1;
      transform: scale(1.4);
    }
  }

  /* --- T·A·H monogram --- */
  .monogram-link { color: var(--color-text-primary); }
  .monogram {
    position: relative;
    display: inline-block;
    font-family: var(--font-display);
    font-weight: 400;
    font-size: 1.35rem;
    line-height: 1;
    letter-spacing: normal;
    color: var(--color-text-primary);
    white-space: nowrap;
  }
  .mono-ch { display: inline-block; }

  .monogram::after {
    content: "";
    position: absolute;
    top: -3px;
    bottom: -3px;
    left: 0;
    width: 2px;
    border-radius: 2px;
    background: linear-gradient(to bottom, transparent, var(--color-accent), transparent);
    opacity: 0;
    pointer-events: none;
  }

  @media (prefers-reduced-motion: no-preference) {
    .mono-ch {
      opacity: 0;
      animation: mono-in 0.5s ease forwards;
      animation-delay: calc(var(--i) * 0.07s);
    }
  }
  @media (prefers-reduced-motion: no-preference) and (pointer: fine) {
    .monogram-link:hover .monogram::after {
      animation: mono-scan 0.55s ease;
    }
  }
  @keyframes mono-in {
    from { opacity: 0; transform: translateY(0.22em); }
    to   { opacity: 1; transform: translateY(0); }
  }
  @keyframes mono-scan {
    0%   { left: -2px; opacity: 0; }
    25%  { opacity: 1; }
    75%  { opacity: 1; }
    100% { left: 100%; opacity: 0; }
  }
</style>

<script>
  // Instrument-console header: scroll-progress scan line, condensed state,
  // and a live readout that names the section currently in view.
  (() => {
    const header = document.querySelector('[data-header]') as HTMLElement | null;
    if (!header) return;

    let ticking = false;
    let wasCondensed = false;
    function update() {
      ticking = false;
      const max = document.documentElement.scrollHeight - window.innerHeight;
      const p = max > 0 ? Math.min(1, window.scrollY / max) : 0;
      document.documentElement.style.setProperty('--progress', String(p));
      const condensed = window.scrollY > 32;
      if (condensed !== wasCondensed) {
        header!.classList.toggle('condensed', condensed);
        wasCondensed = condensed;
      }
    }
    window.addEventListener(
      'scroll',
      () => { if (!ticking) { ticking = true; requestAnimationFrame(update); } },
      { passive: true },
    );
    update();

    // Live readout: on the homepage, swap the static route label for
    // whichever section is currently in view. Other routes keep the
    // server-rendered route name as-is (nothing to observe).
    const readout = document.querySelector('[data-readout]') as HTMLElement | null;
    if (readout && window.location.pathname === '/' && 'IntersectionObserver' in window) {
      const sectionLabels: Record<string, string> = {
        home: 'home',
        about: 'about',
        projects: 'work',
        contact: 'contact',
      };
      const sectionIO = new IntersectionObserver(
        (entries) => {
          for (const entry of entries) {
            if (entry.isIntersecting) {
              const id = (entry.target as HTMLElement).id;
              readout.textContent = `— ${sectionLabels[id] ?? id}`;
            }
          }
        },
        { rootMargin: '-40% 0px -55% 0px' },
      );
      document.querySelectorAll('main section[id]').forEach((s) => sectionIO.observe(s));
    }

    const burger = document.getElementById('nav-toggle-btn') as HTMLButtonElement | null;
    const mobileMenu = document.getElementById('mobile-menu');
    if (mobileMenu) mobileMenu.setAttribute('inert', '');
    if (burger && mobileMenu) {
      const backgroundEls = (): HTMLElement[] =>
        Array.from(document.querySelectorAll('main, footer')) as HTMLElement[];
      const setOpen = (open: boolean) => {
        header!.setAttribute('data-open', String(open));
        burger.setAttribute('aria-expanded', String(open));
        mobileMenu.toggleAttribute('inert', !open);
        for (const el of backgroundEls()) el.toggleAttribute('inert', open);
        if (open) {
          requestAnimationFrame(() => (mobileMenu.querySelector('a') as HTMLElement | null)?.focus());
        }
      };
      const isOpen = () => header!.getAttribute('data-open') === 'true';
      burger.addEventListener('click', () => setOpen(!isOpen()));
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && isOpen()) {
          setOpen(false);
          burger.focus();
        }
      });
      document.addEventListener('click', (e) => {
        if (!isOpen()) return;
        if (!header!.contains(e.target as Node)) setOpen(false);
      });
      mobileMenu.addEventListener('click', (e) => {
        const a = (e.target as HTMLElement).closest('a');
        if (a && mobileMenu.contains(a) && isOpen()) setOpen(false);
      });
    }
  })();
</script>
