---
import BaseLayout from './BaseLayout.astro';
import Header from '../components/layout/Header.astro';
import Footer from '../components/layout/Footer.astro';
import CommandPalette from '../components/layout/CommandPalette.astro';
import IntroAnimation from '../components/layout/IntroAnimation.astro';

interface Props {
  title?: string;
  description?: string;
  image?: string;
  ogType?: 'website' | 'profile' | 'article';
  articleJsonLd?: Record<string, unknown>;
}

const { title, description, image, ogType, articleJsonLd } = Astro.props;
---

<BaseLayout title={title} description={description} image={image} ogType={ogType} articleJsonLd={articleJsonLd}>
  <IntroAnimation />
  <a href="#main-content" class="skip-link">Skip to content</a>
  <Header />
  <main id="main-content" class="pt-16">
    <slot />
  </main>
  <Footer />
  <CommandPalette />
</BaseLayout>
