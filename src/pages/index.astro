---
import PageLayout from '../layouts/PageLayout.astro';
import FeaturedStrip from '../components/FeaturedStrip.astro';
import Marginalia from '../components/composition/Marginalia.astro';
import SectionRule from '../components/composition/SectionRule.astro';
import { getCollection } from 'astro:content';
import { siteConfig } from '../data/site';
import { author } from '../data/author';

const writingAll = await getCollection('writing');
const latestWriting = writingAll
  .sort((a, b) => b.data.date.getTime() - a.data.date.getTime())
  .slice(0, 1)[0];
---

<PageLayout description={siteConfig.description}>
  <section class="hero">
    <div class="grid-cartesian">
      <h1 class="hero-name col-1-9" aria-label={author.name}>
        <span class="hero-name-line" aria-hidden="true">Théo</span>
        <span class="hero-name-line" aria-hidden="true">Alessandro</span>
        <span class="hero-name-line hero-name-line--italic" aria-hidden="true">Hermann</span>
      </h1>
      <aside class="hero-aside col-10-13">
        <Marginalia voice="mono">
          Rio · 2026<br />
          Franco-Brazilian engineer
        </Marginalia>
      </aside>
      <p class="hero-positioning col-1-8">
        Researcher and builder. <span class="hero-positioning-em">MIT Senseable Rio.</span>
        Urban science, robotics, and what comes next.
      </p>
    </div>
  </section>

  <div class="page-wrap">
    <FeaturedStrip />

    {
      latestWriting && (
        <section class="writing-section">
          <SectionRule>Writing</SectionRule>
          <div class="grid-cartesian writing-grid">
            <a href={`/writing/${latestWriting.id}`} class="writing-card col-1-9">
              <span class="mono-caption">
                {latestWriting.data.date.toLocaleDateString('en-US', {
                  year: 'numeric',
                  month: 'long',
                  day: 'numeric',
                })}
              </span>
              <h3 class="writing-title">{latestWriting.data.title}</h3>
              <p class="writing-summary">{latestWriting.data.summary}</p>
            </a>
            <aside class="col-10-13 writing-meta">
              <Marginalia voice="mono">Notes & essays — irregular cadence</Marginalia>
            </aside>
          </div>
        </section>
      )
    }
  </div>
</PageLayout>

<style>
  .hero {
    padding-block: clamp(3rem, 9vw, 6rem) var(--s-16);
  }
  .hero .grid-cartesian {
    row-gap: var(--s-12);
  }

  /* Multi-line stacked wordmark.
     Always Fraunces (per CLAUDE.md §2). Third line italic for tension —
     opsz + SOFT engaged at display sizes. */
  .hero-name {
    font-family: var(--font-wordmark);
    font-weight: 400;
    font-size: var(--text-display-xl);
    line-height: 1;
    letter-spacing: -0.022em;
    color: var(--color-ink);
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 0;
    font-variation-settings:
      'opsz' 144,
      'SOFT' 60;
  }
  .hero-name-line {
    display: block;
  }
  .hero-name-line--italic {
    font-style: italic;
    font-variation-settings:
      'opsz' 144,
      'SOFT' 70;
    letter-spacing: -0.018em;
  }

  .hero-aside {
    align-self: end;
    padding-bottom: var(--s-2);
  }
  @media (max-width: 719px) {
    .hero-aside {
      margin-top: var(--s-6);
    }
  }

  .hero-positioning {
    font-family: var(--font-body);
    font-size: var(--text-body-lg);
    line-height: 1.45;
    color: var(--color-ink-soft);
    margin: 0;
  }
  .hero-positioning-em {
    color: var(--color-ink);
    font-weight: 500;
  }

  .writing-section {
    margin-top: var(--space-section);
  }
  .writing-grid {
    padding-inline: 0;
    max-width: none;
  }

  .writing-card {
    display: block;
    text-decoration: none;
    padding: var(--s-4) 0;
    border-top: 1px solid var(--color-rule);
    border-bottom: 1px solid var(--color-rule);
    color: var(--color-ink);
    transition: border-color 250ms ease;
  }
  .writing-card:hover {
    border-color: var(--color-rule-strong);
  }
  .writing-title {
    font-family: var(--font-cartesian);
    font-size: var(--text-display-sm);
    font-weight: 600;
    margin-top: var(--s-2);
    letter-spacing: -0.015em;
  }
  .writing-card:hover .writing-title {
    color: var(--color-accent);
  }
  .writing-summary {
    color: var(--color-ink-muted);
    margin-top: var(--s-2);
    max-width: 62ch;
    font-size: var(--text-body-sm);
  }
  .writing-meta {
    align-self: center;
  }
</style>
