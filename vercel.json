{
  "buildCommand": "curl -L https://github.com/gohugoio/hugo/releases/download/v0.136.5/hugo_extended_0.136.5_linux-amd64.tar.gz | tar -xz -C /tmp && /tmp/hugo --gc --minify",
  "outputDirectory": "public",
  "devCommand": "hugo server",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ."
}
